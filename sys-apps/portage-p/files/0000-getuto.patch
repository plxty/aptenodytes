commit 041de9fea95bd946c1ff38d9e3b0b4aa810745e2
Author: Zigit Zo <byte@kei.network>
Date:   Wed Apr 22 20:05:50 2026 +0800

    getuto: adding prefix

diff --git a/Makefile b/Makefile
index 9bcc365..e1e5d52 100644
--- a/Makefile
+++ b/Makefile
@@ -1,4 +1,7 @@
 all:
+	mv getuto getuto.old
+	envsubst '$${EPREFIX}' < getuto.old > getuto
+	chmod +x getuto
 
 libc-1-r1-1.gpkg.tar:
 	test -f $@ || wget https://distfiles.gentoo.org/releases/amd64/binpackages/23.0/x86-64/virtual/libc/libc-1-r1-1.gpkg.tar -O libc-1-r1-1.gpkg.tar
diff --git a/getuto b/getuto
index fcdb42b..3728e58 100755
--- a/getuto
+++ b/getuto
@@ -19,7 +19,9 @@
 # sys-apps/gentoo-functions
 
 [[ ${PORTAGE_DEBUG} || ${GETUTO_DEBUG} ]] && set -x
-[[ $(whoami) == 'root' ]] || { echo "${0} must be run as root!" ; exit 100 ; }
+if [[ $(whoami) != 'root' ]]; then
+	ROOT="${EPREFIX}${ROOT}"
+fi
 
 export GNUPGHOME="${ROOT%/}"/etc/portage/gnupg
 
